/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lab-san-rafael-api.onrender.com'], // Agrega aquí el dominio de la imagen
      },
};

module.exports = nextConfig;
